  view:
    default:
      root: /usr/local
      exclude: [epic]
      link_type: symlink
    detectors:
      root: /opt/detector
      select: [epic]
      projections:
        all: '{name}-{version}'
      link: roots
      link_type: symlink
